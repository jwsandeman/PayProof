<h1>Edit Review</h1>

<%= simple_form_for([@job, @review]) do |f| %>
    <%= f.input :rating, required: true, collection: 1..5 %>
    <%= f.input :comment, required: true %>
    <%= f.button :submit, label: "Update Review", class: "btn btn-outline-success btn-md" %>
<% end %>